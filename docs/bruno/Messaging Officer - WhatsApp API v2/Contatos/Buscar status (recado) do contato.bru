meta {
  name: Buscar status (recado) do contato
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/status/:number
  body: none
  auth: inherit
}

params:path {
  number: 
}

headers {
  X-Session-Id: {{sessionId}}
}

settings {
  encodeUrl: true
}
