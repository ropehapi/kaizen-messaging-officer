meta {
  name: Reiniciar sessão
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/sessions/:sessionId/restart
  body: none
  auth: inherit
}

params:path {
  sessionId: pedro
}

settings {
  encodeUrl: true
}
