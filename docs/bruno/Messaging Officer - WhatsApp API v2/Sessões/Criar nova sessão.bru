meta {
  name: Criar nova sessão
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/sessions
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  x-api-key: {{x-api-key}}
}

body:json {
  {
    "sessionId": "pedro"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
